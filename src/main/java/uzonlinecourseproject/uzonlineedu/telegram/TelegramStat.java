package uzonlinecourseproject.uzonlineedu.telegram;

public enum TelegramStat {
    STARTED,
    SELECT_COURSE,
    SELECT_FREE,
    PAYMENT_,
    AWAITING_EMAIL_CHECK,
    AWAITING_USERNAME,
    AWAITING_EMAIL,
    AWAITING_PASSWORD
}
